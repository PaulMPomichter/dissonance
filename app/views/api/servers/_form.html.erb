<%= form_with(model: api_server, local: true) do |form| %>
  <% if api_server.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_server.errors.count, "error") %> prohibited this api_server from being saved:</h2>

      <ul>
      <% api_server.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
