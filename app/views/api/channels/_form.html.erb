<%= form_with(model: api_channel, local: true) do |form| %>
  <% if api_channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_channel.errors.count, "error") %> prohibited this api_channel from being saved:</h2>

      <ul>
      <% api_channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
