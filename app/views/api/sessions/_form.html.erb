<%= form_with(model: api_session, local: true) do |form| %>
  <% if api_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_session.errors.count, "error") %> prohibited this api_session from being saved:</h2>

      <ul>
      <% api_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
